from distutils.core import setup

setup(name='quaterion',
      version='1.0',
      description='Python quaternion library',
      author='Jost Prevc',
      author_email='jost.prevc@gmail.com',
      packages=['quaternion']
      )